# OpenShift Cluster Credentials Template
# Copy to: openshift/secrets/cluster-credentials.env (gitignored)
# Populate with your cluster credentials.
#
# DO NOT commit the populated file. Only this template is committed.

OPENSHIFT_API_URL=https://api.your-cluster.example.com:6443
OPENSHIFT_TOKEN=REPLACE_ME
OPENSHIFT_NAMESPACE=pragma-encoder
